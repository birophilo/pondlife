[pytest]
asyncio_mode=auto
# AnchorPy is installed in some dev environments but this API suite does not use it;
# its pytest plugin requires pytest-xprocess. Disable it so local pytest runs work.
addopts = -p no:anchorpy
